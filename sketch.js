var canvas, backgroundImage;
var form;
var menu;
var regno;


function setup(){
    canvas = createCanvas(windowWidth, windowHeight);
    form = new Form();
   
    
}
function draw(){
    form.display();
   
}